You are a Principal Software Architect, Staff Backend Engineer, PostgreSQL Expert, DevOps Engineer, Platform Engineer, and Site Reliability Engineer (SRE). Perform a complete production-grade audit of the entire codebase and permanently eliminate every root cause of application crashes, Render health check failures, PostgreSQL `ECONNABORTED` errors, startup instability, restart loops, event-loop blocking, connection leaks, slow health responses, resource contention, memory leaks, CPU spikes, scheduler failures, race conditions, deadlocks, unhandled promise rejections, and uncaught exceptions. Find the exact root cause—not just the symptoms—and implement permanent fixes. Refactor the startup architecture so the HTTP server becomes ready immediately, move all heavy initialization, AI ingestion, database seeding, RSS sync, WebSub, vector indexing, cron jobs, media processing, and background workers to safe asynchronous post-start execution with bounded concurrency. Replace unsafe database usage with a resilient shared PostgreSQL connection pool, automatic reconnection, keepalive, connection validation, idle and connection timeouts, query optimization, retries with exponential backoff for transient failures, and circuit breakers where appropriate. Ensure every scheduler and worker is fully isolated so no background failure can ever terminate the API process. Implement production-grade readiness and liveness endpoints that always respond in under 100 ms, comprehensive monitoring, structured logging, metrics, graceful startup/shutdown, automatic recovery, and self-healing behavior. Optimize the application for high availability, reliability, scalability, and continuous uptime. Eliminate every issue preventing Render from passing health checks and ensure Googlebot and Google AdSense always receive reliable HTTP 200 responses. Preserve all existing functionality, APIs, business logic, and data integrity. Do not use temporary workarounds—implement permanent production-quality solutions. After completion, provide the exact root cause of every issue, every code change made, why each fix is permanent, and evidence through stress testing and health-check validation that the platform can run continuously without crashes, restarts, or downtime.
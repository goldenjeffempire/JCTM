Senior AI Engineer and Full-Stack Systems Architect.

Objective: Rebuild the "TempleBots" infrastructure from scratch. Develop a Local AI Orchestration Layer using OpenAI’s GPT-4o models, ensuring the chatbot is fully operational, production-ready, and grounded in JCTM-specific data.

1. The Local AI Engine (Code Architecture):

Framework: Build the core logic using LangChain or LlamaIndex within a Node.js/Next.js environment.

Vector Store (The "Local" Brain): Implement a local Vector Database (using pgvector on Supabase or Pinecone).

Ingestion Script: Write a local script to "Chunk" and "Embed" the text from the JCTM website, the Warri City Crusade 2026 details, and all Temple TV transcripts.

2. OpenAI Model Integration:

Model Choice: Use gpt-4o for high-reasoning theological responses.

Embedding Model: Use text-embedding-3-small to convert JCTM doctrines into mathematical vectors for local searching.

System Prompt Hardening: Inject a master "System Message":
"You are TempleBots, the official AI for Jesus Christ Temple Ministry. You speak with the authority and compassion of Prophet Amos Evomobor. Your knowledge is strictly grounded in the provided JCTM transcripts and the 'Correction Mandate.' If a user asks about the Warri City Crusade, provide the exact April 30th - May 1st details. If you don't know an answer based on JCTM doctrine, refer them to the Temple TV YouTube channel."

3. Production-Ready Functionalities:

Streaming Responses: Implement Server-Sent Events (SSE) so the AI text appears to "type" in real-time, matching the futuristic UI.

Conversation Memory: Use a local session_id to store chat history in the database, allowing TempleBots to remember the user's name and previous prayer requests.

Contextual Tooling: Enable the AI to "trigger" actions, such as showing the "Sow a Seed" button if a user mentions giving.

4. Deployment & Real-World Hardening:

Rate Limiting: Implement a local "Throttling" logic to prevent API abuse and manage OpenAI costs.

Error Handling: Build a graceful fallback—if the AI is offline, show a "Contact Ministry" button.

Render Integration: Ensure all OPENAI_API_KEY and database secrets are properly configured for the Render.com production environment.

Execute the complete AI rebuild and deploy the code now.